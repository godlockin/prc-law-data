# 共享规则与官方核验 · 2026-09-09

`law_common/`为核心条号、版本选择、配置、HTTP基础、资料生命周期、身份限流、成本账本及官方页面核验的单一源码。PRC-Law消费其wheel，修改后须重新生成并同步wheel及哈希清单：

```sh
python3 scripts/package_common.py --destination /path/to/PRC-Law/vendor
python3 -m pip install /path/to/PRC-Law/vendor/prc_law_common-1.0.0-py3-none-any.whl
```

包固定依赖`cryptography==50.0.0`。使用同一个Python安装依赖、启动服务和运行测试；CI按此执行。统一配置读取`PRC_LAW_CONFIG`，环境变量可覆盖，未知字段及非法值拒绝启动。

`scripts/official_updates.py`提供显式check/approve入口；限定官方HTTPS来源、响应大小和跳转，保存页面哈希及复核记录。失败不刷新成功时间，变化/过期需要复核。固定页面的审核通过也不证明所有修法公告已发现、法条目前有效或适用于具体案件；状态保持`official_current_validity_proven:false`。

本次实际候选政府页面返回404，人大候选链接连接失败；没有成功取得官方现行效力证明。尚需定位可访问具体来源并完成法源、修订废止及适用日期复核。未开启定时维护或自动公告更新。

成本仅记录已接入路由的供应商调用，空账本及未知单价不记作零成本。真实账单/定价、服务主体、人工负责人及完整服务成本仍待核实。旧资料目录和外部备份不自动纳入加密资料库的删除范围。

强杀/断电/长期运行、专家标注案例集、实际告警/异机恢复/自动维护按用户要求暂不开展。数据恢复报告属于构建记录，不等于法律准确性认证或新检出法律全文的再分发授权。

本机恢复记录：来源记录26,079、重建版本24,810、重建条文963,710、索引文件24,894；仍有84个旧文件未重建、5条来源记录拒绝、162条来源记录未结构化。详细`data/recovery-report.json`为可再生成的本机构建产物，与全文/索引一起忽略；本仓库保留此摘要和恢复脚本。
